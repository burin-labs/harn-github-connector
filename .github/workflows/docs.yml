name: Documentation

on:
  pull_request:
  push:
    branches: [main]
  merge_group:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  examples:
    name: Harn documentation examples
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Check README examples
        uses: burin-labs/.github/.github/actions/check-harn-docs@d399b007b53c3d319ceb0bcd5942b745aef3f967
